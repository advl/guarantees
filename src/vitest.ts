/**
 * @module
 *
 * The `./vitest` entry point: the runner configuration a corpus's own
 * `vitest.config.ts` hands to the runner, and nothing else.
 */
export { defineCorpusConfig } from "./lib/index.js";
