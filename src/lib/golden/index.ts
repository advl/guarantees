/**
 * @module
 *
 * A golden's update path: what a run generated inside its image, lifted
 * over what the tree holds, with what changed and what the run no longer
 * generates answered rather than printed. The one path by which a golden
 * changes; a missing golden is the entry's failure and never this domain's.
 */
export { default as acceptGolden } from "./acceptGolden.js";
export { GOLDENS_DIR } from "./constants.js";
export type { Accepted } from "./types.js";
