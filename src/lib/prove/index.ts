/**
 * @module
 *
 * What a tier's proof is: its row designed to fail, watched going red, and
 * — where the tier holds the bijection — that scan watched finding a file
 * no row claims, by the name of the assertion that reports it and read from
 * the run's own lifted report. Starts containers and writes in the corpus
 * tree, and restores it on every path out.
 */
export { BIJECTION_ID, ORPHAN_ID } from "./constants.js";
export { default as proveTier } from "./proveTier.js";
export type { ProveContext } from "./types.js";
