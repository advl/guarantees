{
  "name": "@aztlan/guarantees",
  "description": "The scheduler for a repository's guarantees: a register of rows, each run in a digest-pinned container under the repository's own test runner, and a verdict",
  "version": "0.0.0",
  "type": "module",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "default": "./dist/esm/index.js"
    },
    "./vitest": {
      "types": "./dist/types/vitest.d.ts",
      "default": "./dist/esm/vitest.js"
    },
    "./contract": {
      "types": "./dist/types/contract.d.ts",
      "default": "./dist/esm/contract.js"
    },
    "./contract/*.schema.json": "./contract/*.schema.json"
  },
  "files": [
    "dist",
    "contract"
  ],
  "sideEffects": false,
  "author": {
    "email": "47156835+advl@users.noreply.github.com",
    "name": "advl"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/advl/guarantees"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/advl/guarantees/issues"
  },
  "homepage": "https://github.com/advl/guarantees#readme",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && bun run build:contract",
    "build:contract": "bun src/scripts/emitSchemas.ts",
    "check": "bun run build && bun run check:biome && bun run check:ts && bun run check:leakage && bun run check:webarchitect && bun run check:structure && bun run check:contract",
    "check:fix": "bun run check:biome:fix && bun run check:ts",
    "check:biome": "biome check",
    "check:biome:fix": "biome check --write",
    "check:ts": "tsc --noEmit",
    "check:webarchitect": "webarchitect library --license MIT --prefix '@aztlan/'",
    "check:leakage": "bun scripts/check-leakage.ts",
    "check:structure": "bun scripts/check-structure.ts",
    "check:contract": "bun scripts/check-contract.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "ci": "bun run check && bun run test:coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.9",
    "@canonical/biome-config": "0.33.0",
    "@canonical/typescript-config": "0.33.0",
    "@canonical/webarchitect": "0.33.0",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "peerDependencies": {
    "vitest": "^4"
  },
  "dependencies": {
    "smol-toml": "1.7.1"
  }
}
